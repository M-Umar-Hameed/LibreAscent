#!/usr/bin/env sh

# Windows nvm fix: use node directly to run lint-staged.
# lint-staged config lives in mobile/; run from there so paths resolve.
cd mobile && node ./node_modules/lint-staged/bin/lint-staged.js
